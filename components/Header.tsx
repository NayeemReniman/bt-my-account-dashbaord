import { SiteHeader } from "@arc-ui/components";
import { FunctionComponent } from "react";

interface HeaderProps {}

const Header: FunctionComponent<HeaderProps> = () => {
  return <SiteHeader></SiteHeader>;
};

export default Header;
